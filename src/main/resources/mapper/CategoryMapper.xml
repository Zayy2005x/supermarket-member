<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zayy.supermarketmember.mapper.CategoryMapper">
    <update id="update">
        UPDATE supermarket_member.category
        <set>
            <if test="name != null and name !='' ">name=#{name},</if>
            <if test="description != null">description=#{description},</if>
            <if test="icon != null">icon=#{icon},</if>
            <if test="status != null">status=#{status},</if>
            <if test="createTime != null">create_time=#{createTime},</if>
            <if test="createId != null">create_id=#{createId},</if>
            <if test="updateTime != null">update_time=#{updateTime},</if>
            <if test="updateId != null">update_id=#{updateId}</if>
        </set>
        WHERE id=#{id}
    </update>
    <select id="page" resultType="com.zayy.supermarketmember.pojo.entity.Category">
        SELECT * FROM supermarket_member.category
        <where>
            <if test="name != null and name != ''">
                name=#{name}
            </if>
        </where>
    </select>
</mapper>